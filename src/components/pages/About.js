import React from 'react'

const About = (props) => {
	return (
		<div className="container mt-3">
			<h1>About The Website</h1>
			<p className="lead">This web app is used to search for github users using their name and see general information about them.</p>
		</div>
	)
}

export default About
